# admin

Vue3 + Vite + TypeScript + Element Plus 管理端骨架。当前 M0 提供空登录页占位，AI 渠道配置、提示词模板、敏感词、解梦记录等功能在后续里程碑实现。

## 本地启动

```bash
cd admin
npm install
npm run dev
```

默认开发地址：`http://localhost:5173`

## 构建验证

```bash
npm run build
```

参见 `../docs/api-contract.md`（管理端接口）。
